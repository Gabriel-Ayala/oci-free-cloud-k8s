apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  # Minimal profile: keep the initial cluster light and independent of
  # Vault, DNS, domains, and application-specific credentials.
  - metrics-server/flux.yaml
