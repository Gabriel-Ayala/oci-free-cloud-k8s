apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-hackyard-dev
spec:
  dnsNames:
    - "*.hackyard.dev"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: wildcard-hackyard-dev-tls-cert
